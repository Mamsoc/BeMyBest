<%= simple_form_for([@project, @memory]) do |f| %>
  <%= f.input :photo, as: :file %>
  <%= f.input :legend, label: "Description de votre souvenir" %>
  <%= f.submit class: "btn btn-flat", value: "Ajouter" %>
<% end %>
