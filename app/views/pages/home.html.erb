<div class="home-banner">
  <p class="line-1 anim-typewriter">La vie est un jeu</p>
  <% if @current_user_projects.empty? %>

      <%= link_to "GO", new_project_path, class: "btn banner-btn fw-bold h2"%>

      <%= link_to "GO", new_project_path, class: "btn banner-btn fw-bold"%>

    <% else %>
      <%= link_to "GO", projects_path, class: "btn banner-btn fw-bold"%>
    <% end %>
</div>
